# Backend Configuration
# Default: sqlite:///./campus_store.db
# DATABASE_URL=postgresql://user:password@aws-0-region.pooler.supabase.com:6543/postgres

# Allow Frontend URL for CORS (comma separated)
CORS_ORIGINS=http://localhost:5173,https://your-vercel-app.vercel.app

# Server Port (Optional, default 8000)
PORT=8000

# Frontend Configuration (Only for Vite/Vercel)
# Leave empty for local development (uses proxy)
# VITE_API_URL=https://your-railway-app.up.railway.app
